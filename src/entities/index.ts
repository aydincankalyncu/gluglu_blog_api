import { Category } from "./category.entity";
import { Post } from "./post.entity";
import { Comment } from "./comment.entity";
import { User } from "./user.entity";

const entities = [Post, Category, Comment, User];

export default entities;